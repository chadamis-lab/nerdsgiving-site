import React from "react"
import ReactDOM from "react-dom/client"
import NerdsgivingPage from "./NerdsgivingPage"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NerdsgivingPage />
  </React.StrictMode>
)
